server {
    listen 8080;
    proxy_pass 10.100.100.200:211;

    proxy_connect_timeout 60s;
    proxy_timeout 60s;


    # 正確 stream access_log 寫法
    access_log off;
    error_log /var/log/nginx/stream_error.log warn;
}